name: NOTSIP CI
on:
  push:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - name: Install
        run: python -m pip install -e '.[dev]'
      - name: Compile
        run: python -m compileall -q src
      - name: Test
        run: pytest -q
      - name: Docker build
        run: docker build -t notsip-ci .
  windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - name: Install
        shell: pwsh
        run: python -m pip install -e '.[windows]'
      - name: Build executable
        shell: pwsh
        run: .\scripts\build_exe.ps1
      - name: Smoke actual NOTSIP.exe end-to-end
        shell: pwsh
        env:
          NOTSIP_HOST: '127.0.0.1'
          NOTSIP_PORT: '8765'
          NOTSIP_API_KEY: ''
          NOTSIP_AUTH_MODE: 'api_key'
          NOTSIP_DATA_DIR: '${{ github.workspace }}\\ci-data'
          NOTSIP_OPEN_BROWSER: 'false'
        run: |
          $ErrorActionPreference = 'Stop'
          $base='http://127.0.0.1:8765'
          New-Item -ItemType Directory -Force -Path "$env:NOTSIP_DATA_DIR" | Out-Null
          $p = Start-Process -FilePath '.\dist\NOTSIP.exe' -PassThru -WindowStyle Hidden -RedirectStandardOutput "$env:NOTSIP_DATA_DIR\notsip.stdout.log" -RedirectStandardError "$env:NOTSIP_DATA_DIR\notsip.stderr.log"
          try {
            $ready = $false
            1..40 | ForEach-Object {
              if(-not $ready) {
                try { $r = Invoke-WebRequest -Uri "$base/" -UseBasicParsing -TimeoutSec 2 -MaximumRedirection 5; if($r.StatusCode -eq 200 -and $r.Content -match 'NOTSIP'){ $ready=$true } } catch {}
                if(-not $ready){Start-Sleep -Milliseconds 500}
              }
            }
            if(-not $ready){Write-Host '--- stdout ---';Get-Content "$env:NOTSIP_DATA_DIR\notsip.stdout.log" -ErrorAction SilentlyContinue;Write-Host '--- stderr ---';Get-Content "$env:NOTSIP_DATA_DIR\notsip.stderr.log" -ErrorAction SilentlyContinue;throw 'NOTSIP.exe did not start'}
            $setup=Invoke-WebRequest "$base/setup" -UseBasicParsing -TimeoutSec 5
            if($setup.StatusCode -ne 200 -or $setup.Content -notmatch 'NOTSIP first-run setup'){throw 'First-run setup UI failed'}
            $body='{"settings":{"local_timezone":"Africa/Kigali","autonomy_level":1,"vision_enabled":false,"perception_enabled":false,"browser_enabled":false,"checkpoint_interval":60,"health_interval":5,"proactive_interval":30,"memory_maintenance_interval":120}}'
            try {$cfg=Invoke-WebRequest -Uri "$base/api/config" -Method POST -ContentType 'application/json' -Body $body -UseBasicParsing -TimeoutSec 5} catch {Write-Host '--- config response ---';try{Write-Host $_.ErrorDetails.Message}catch{};Write-Host '--- stdout ---';Get-Content "$env:NOTSIP_DATA_DIR\notsip.stdout.log" -ErrorAction SilentlyContinue;Write-Host '--- stderr ---';Get-Content "$env:NOTSIP_DATA_DIR\notsip.stderr.log" -ErrorAction SilentlyContinue;throw}
            if($cfg.StatusCode -ne 200 -or $cfg.Content -notmatch '"status":"SUCCESS"'){throw 'Minimal configuration failed'}
            $state=Invoke-WebRequest "$base/api/config/public" -UseBasicParsing -TimeoutSec 5
            if($state.StatusCode -ne 200 -or $state.Content -notmatch '"autonomy_level":1' -or $state.Content -notmatch '"vision_enabled":false'){throw 'Saved configuration could not be read back'}
            $console=Invoke-WebRequest "$base/" -UseBasicParsing -TimeoutSec 5
            if($console.StatusCode -ne 200 -or $console.Content -notmatch 'NOTSIP</b>'){throw 'Configured console failed'}
            foreach($path in @('/api/health','/api/status','/api/diagnostics','/api/capabilities','/api/backups','/api/approvals','/api/devices','/api/tasks','/api/sessions','/api/integrations/accounts','/api/update/check')){
              $r=Invoke-WebRequest "$base$path" -UseBasicParsing -TimeoutSec 5
              if($r.StatusCode -notin @(200,503)){throw "Endpoint failed: $path -> $($r.StatusCode)"}
            }
            Stop-Process -Id $p.Id -Force
            $p.WaitForExit()
            Start-Sleep -Milliseconds 500
            $p=Start-Process -FilePath '.\dist\NOTSIP.exe' -PassThru -WindowStyle Hidden -RedirectStandardOutput "$env:NOTSIP_DATA_DIR\notsip.restart.stdout.log" -RedirectStandardError "$env:NOTSIP_DATA_DIR\notsip.restart.stderr.log"
            $ready=$false
            1..40|ForEach-Object{if(-not $ready){try{$r=Invoke-WebRequest "$base/" -UseBasicParsing -TimeoutSec 2;if($r.StatusCode -eq 200 -and $r.Content -match 'NOTSIP'){$ready=$true}}catch{};if(-not $ready){Start-Sleep -Milliseconds 500}}}
            if(-not $ready){Write-Host '--- restart stdout ---';Get-Content "$env:NOTSIP_DATA_DIR\notsip.restart.stdout.log" -ErrorAction SilentlyContinue;Write-Host '--- restart stderr ---';Get-Content "$env:NOTSIP_DATA_DIR\notsip.restart.stderr.log" -ErrorAction SilentlyContinue;throw 'NOTSIP.exe did not restart'}
            $persisted=Invoke-WebRequest "$base/api/config/public" -UseBasicParsing -TimeoutSec 5
            if($persisted.Content -notmatch '"autonomy_level":1' -or $persisted.Content -notmatch '"vision_enabled":false' -or $persisted.Content -notmatch '"perception_enabled":false'){throw 'Configuration was not retained across restart'}
            $p2=Start-Process -FilePath '.\dist\NOTSIP.exe' -PassThru -WindowStyle Hidden
            Start-Sleep -Seconds 2
            if(-not $p2.HasExited){Stop-Process -Id $p2.Id -Force;throw 'Second NOTSIP.exe instance remained running'}
            if($p2.ExitCode -ne 0){throw "Second launch returned $($p2.ExitCode)"}
            $live=Invoke-WebRequest "$base/api/health" -UseBasicParsing -TimeoutSec 5
            if($live.StatusCode -ne 200){throw 'Existing NOTSIP service was not left healthy after duplicate launch'}
          } finally {if($p -and -not $p.HasExited){Stop-Process -Id $p.Id -Force}}
      - name: Build installer
        shell: pwsh
        run: |
          choco install innosetup --yes --no-progress
          & 'C:\Program Files (x86)\Inno Setup 6\ISCC.exe' .\scripts\NOTSIP.iss
          if(-not(Test-Path 'Output\NOTSIP-Setup.exe')){throw 'Installer was not produced'}
      - name: Smoke installed NOTSIP-Setup.exe end-to-end
        shell: pwsh
        env:
          NOTSIP_HOST: '127.0.0.1'
          NOTSIP_PORT: '8776'
          NOTSIP_API_KEY: ''
          NOTSIP_AUTH_MODE: 'api_key'
          NOTSIP_OPEN_BROWSER: 'false'
        run: |
          $ErrorActionPreference='Stop'
          $installDir=Join-Path $env:RUNNER_TEMP 'NOTSIP-installed'
          if(Test-Path $installDir){Remove-Item $installDir -Recurse -Force}
          Start-Process -FilePath '.\Output\NOTSIP-Setup.exe' -ArgumentList "/VERYSILENT","/SUPPRESSMSGBOXES","/NORESTART","/DIR=$installDir" -Wait
          if(-not(Test-Path "$installDir\NOTSIP.exe")){throw 'Installed NOTSIP.exe missing'}
          $env:NOTSIP_DATA_DIR=Join-Path $installDir 'data'
          New-Item -ItemType Directory -Force -Path $env:NOTSIP_DATA_DIR | Out-Null
          $p=Start-Process -FilePath "$installDir\NOTSIP.exe" -PassThru -WindowStyle Hidden
          try {
            $ready=$false
            1..30|ForEach-Object{if(-not $ready){try{$r=Invoke-WebRequest 'http://127.0.0.1:8776/' -UseBasicParsing -TimeoutSec 2;if($r.StatusCode -in @(200,302)){$ready=$true}}catch{};if(-not $ready){Start-Sleep -Milliseconds 500}}}
            if(-not $ready){throw 'Installed NOTSIP.exe did not start'}
            foreach($u in @('http://127.0.0.1:8776/api/health','http://127.0.0.1:8776/api/status')){$r=Invoke-WebRequest $u -UseBasicParsing -TimeoutSec 5;if($r.StatusCode -ne 200){throw "Installed endpoint failed: $u"}}
          } finally {if($p -and -not $p.HasExited){Stop-Process -Id $p.Id -Force}}
          $uninstaller=Join-Path $installDir 'unins000.exe';if(Test-Path $uninstaller){Start-Process -FilePath $uninstaller -ArgumentList '/VERYSILENT','/NORESTART' -Wait}
      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: NOTSIP-Windows
          path: |
            dist/NOTSIP.exe
            Output/NOTSIP-Setup.exe
  android-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: {distribution: temurin, java-version: '17'}
      - uses: gradle/actions/setup-gradle@v4
        with: {gradle-version: '8.13'}
      - name: Android assemble
        run: gradle -p android assembleDebug --no-daemon