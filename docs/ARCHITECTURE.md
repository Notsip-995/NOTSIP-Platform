# NOTSIP architecture

Input/context -> memory/world -> provider -> tool planning -> policy -> action -> verification/audit -> memory/event continuation.

Windows is the primary execution node. Android is a paired subordinate node with a device-scoped token. Provider credentials remain on the Windows node.
