# NOTSIP Android companion 0.7

Pairs to NOTSIP using a short-lived code, stores only a device-scoped token, sends heartbeats, polls commands and exposes user-enabled AccessibilityService automation. Provider API keys never leave the Windows node.
